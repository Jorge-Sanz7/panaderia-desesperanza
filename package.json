{
  "name": "panaderia-desesperanza",
  "version": "1.0.0",
  "description": "Sistema de gestión para panadería",
  
  "main": "src/main.js",
  "scripts": {
    
    // 🎯 CORRECCIÓN CLAVE: Eliminamos 'src/' para compensar la duplicación de ruta en Render.
    "start": "node main.js", 
    
    // Este script se mantiene con 'src/' porque es para uso local donde sí funciona la ruta completa.
    "dev": "nodemon src/main.js",
    "build": "echo \"Nada que construir, es una app Node.js con Express\"",
    "test": "jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
